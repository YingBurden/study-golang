学习工厂模式
在写这个方法的时候，我的想法是接口继承，写着写着发现不对，发现工厂模式只能适用于接口实现，也就是不能出现非接口的方法，所以也就是现在呈现的demo
我本可以重新换思路，换个更能实现工厂方法的例子的，但我决定留着，正确的case有无数个，但错误的case更能让人铭记


总结：
1.工厂模式只能满足接口实现，不能够实现接口继承，所以也只能使用于这种场景
2.Golang里大写的方法才可被外部调用，不管是不是结构体的实现方法

